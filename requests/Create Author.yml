info:
  name: Create Author
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/authors
  body:
    type: json
    data: |-
      {
        "name": "John R. R. Tolkien",
        "country": "United Kingdom"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
