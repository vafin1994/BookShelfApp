info:
  name: Create Author
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/authors
  body:
    type: json
    data: |-
      {
        "name": "Robert C. Martin",
        "country": "USA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
